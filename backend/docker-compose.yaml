services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.dev
    restart: always
    ports:
      - '6001:6001'
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
    networks:
      - network

networks:
  network:
    external: true
